"use client";

// Entrance animations across the site read this to decide when to play.
// The branded intro loader has been removed, so content is always ready
// immediately — this stub keeps the existing call sites unchanged.
export function usePageReady(): boolean {
  return true;
}
